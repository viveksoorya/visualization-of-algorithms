The program reads the JSON file, computes the density matrix for each stage (ρ=∣ψ⟩⟨ψ∣ρ=∣ψ⟩⟨ψ∣), and creates a 3D bar chart with two subplots (real part and imaginary part). It uses NumPy for linear algebra and Matplotlib for 3D plotting.

Dependencies:

- Python 3.7+
- NumPy
- Matplotlib


Install with
```
pip install numpy matplotlib
```
